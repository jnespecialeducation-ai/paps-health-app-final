{
  "buildCommand": "node scripts/prepare-schema.js && prisma generate && prisma migrate deploy && next build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["icn1"],
  "env": {
    "PRISMA_GENERATE_DATAPROXY": "false"
  }
}
